import makeStyles from "@mui/styles/makeStyles";

export default makeStyles(
  () => ({
    root: {
      textAlign: "center",
      backgroundColor: "red"
    }
  }),
  { name: "CalendarDetailModal" },
);
